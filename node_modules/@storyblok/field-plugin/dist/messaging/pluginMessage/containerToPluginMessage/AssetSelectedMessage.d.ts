import { MessageToPlugin } from './MessageToPlugin';
import { Asset } from './Asset';
export type AssetSelectedMessage = MessageToPlugin<'asset-selected'> & {
    field?: string;
} & Asset & Record<string, unknown>;
export declare const isAssetSelectedMessage: (data: unknown) => data is AssetSelectedMessage;
export declare const hasField: (data: unknown) => boolean;
